// If there is an error loading the data, maximum number of retries and time between them
export const MAX_RETRY_NUMBER = 5;
export const TIME_RETRY = 1000;
// Population value Max length
export const MAX_POPULATION_LENGTH = 30;
