/* Pseudo tests for illustrative purposes */
describe('<PopulationsForm />', () => {
  it(`renders a form with a title, dropdown select of countries, population input field, submit button, and an empty
    "countries by population" list with placeholder when the component mounts`, () => {
    // do stuff
  });
  it(`when form is submitted with population for a new country the country gets added to the "countries by population"
    list and sorted by population in descending order`, () => {
    // do stuff
  });
  it(`when form is submitted with changed population for an existing country the "countries by population" list is
    updated with the new value and sorted by population in descending order`, () => {
    // do stuff
  });
  it('when delete button is clicked the relevant country is removed from the "countries by population" list', () => {
    // do stuff
  });
});
